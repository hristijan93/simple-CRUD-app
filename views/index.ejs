<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My simple CRUD app</title>
</head>
<body>
    <h1>May Node and Expreses be with you.</h1>

    <!-- CRUD - CREATE // Triggering POST request through a form -->
    <form action="/quotes" method="POST">
        <input type="text" placeholder="name" name="name">
        <input type="text" placeholder="quote" name="quote">
        <button type="submit">Submit</button>
    </form>
    <h2>Quotes</h2>

    <ul class="quotes">
        <!-- Loop through quites -->
        <% for (let i = 0; i < quotes.length; i++) {%>
            <li class='quote'>
                <span><b><%= quotes[i].name %></b></span>:
                <span><%= quotes[i].quote %></span>
            </li>
        <% } %>
    </ul>

    <div>
        <h2>Darth Vader invades!</h2>
        <p>Replacee first Yoda's quote with a quote written by Darth Vader</p>
        <button id="update-button">Replace Yoda's quote</button>
    </div>
</body>
</html>